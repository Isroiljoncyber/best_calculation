import 'package:flutter/material.dart';

class ThemesTab extends StatelessWidget {
  const ThemesTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text("Themes"),
      ),
    );
  }
}
